// Example: Replace with actual API calls later
export async function getPatientData(patientId) {
  // For now, return mock data
  return {
    id: patientId,
    armMovement: [30, 35, 33, 37, 40],
    legStepHeight: [10, 12, 11, 14, 15],
  };
}
